# Testcontainers configuration for Podman
# Podman exposes Docker-compatible API via named pipe on Windows
docker.host=npipe:////./pipe/docker_engine
ryuk.container.privileged=true
testcontainers.reuse.enable=true
